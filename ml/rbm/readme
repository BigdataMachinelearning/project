1 测试内容

1. 评分个数
2. 隐单元激活特征数
3. 矩检查法，检查参数的一阶矩，二阶，即均值和方差 
4. 测试集与训练集平方误差
5. 经验分布与模型抽样分布保持一致

RBM程序调试经验
1. 数组越界，5分，在实际数组中的位置为0-4
如何检测出该错误，通过抽样测试发现，样本为0-5六个值，
与实际不符合，从而检测出错误来源。
2. 高组数组遍历时边界限制，w矩阵为三维矩阵，
遍历时，边界设置混淆，使得更新矩阵时产生异常。
通过检测矩阵的均值和方差发现异常行为，从而发现错误
来源。

RepSoftMax程序调试经验
1 梯度是否正常运行，每次梯度是否下降
